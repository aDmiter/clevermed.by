# ~/clevermed.by/clevermed-by/.env (только на сервере, не в Git)
# Пароль из панели Beget; символ % в пароле → %25 в URL

DATABASE_URL="mysql://admite_clevereac:ПАРОЛЬ_С_ПАНЕЛИ@localhost:3306/admite_clevereac"

AUTH_SECRET="openssl rand -base64 32"
AUTH_URL="https://clevermed.by"

ADMIN_LOGIN=superadmin
ADMIN_EMAIL=admin@clevermed.by
ADMIN_PASSWORD="надёжный пароль от 12 символов"

NODE_ENV=production
