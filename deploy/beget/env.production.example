# Скопируйте на сервер в .env (рядом с package.json)
# Данные MySQL — из панели Beget → MySQL

DATABASE_URL="mysql://DB_USER:DB_PASSWORD@localhost:3306/DB_NAME"

AUTH_SECRET="сгенерируйте: openssl rand -base64 32"
AUTH_URL="https://clevermed.by"

ADMIN_LOGIN=superadmin
ADMIN_EMAIL=admin@clevermed.by
ADMIN_PASSWORD="надёжный пароль от 12 символов"

NODE_ENV=production
